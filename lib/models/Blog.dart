class Blog {
  String userName;
  String profilePictureUrl;
  int likes;
  bool isLiked;
  String content;
  String image;

  Blog(this.userName, this.profilePictureUrl, this.likes, this.isLiked,
      this.content, this.image);
}
